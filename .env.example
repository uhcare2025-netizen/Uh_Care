# .env.example — UH Care Environment Variables for PythonAnywhere Deployment
# Copy this file to .env locally or set these as environment variables on PythonAnywhere Web tab
# NEVER commit real secrets — use this template only

# ========================
# DJANGO CORE SETTINGS
# ========================
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=your-secure-random-secret-key-here-min-50-chars
DJANGO_ALLOWED_HOSTS=yourusername.pythonanywhere.com,localhost,127.0.0.1

# ========================
# DATABASE
# ========================
# Leave unset for local SQLite (default: db.sqlite3)
# For PythonAnywhere MySQL:
# DATABASE_URL=mysql://yourusername:password@yourusername.mysql.pythonanywhere-services.com/yourusername$dbname
# For Postgres:
# DATABASE_URL=postgresql://user:password@host:port/dbname
DATABASE_URL=

# ========================
# STATIC & MEDIA FILES
# ========================
# Defaults in settings.py use relative paths; no need to set these unless using S3
# STATIC_URL=/static/ (served by WhiteNoise or PythonAnywhere static mapping)
# MEDIA_URL=/media/

# ========================
# AWS S3 (optional, for static/media storage in production)
# ========================
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1
AWS_S3_CUSTOM_DOMAIN=

# ========================
# EMAIL CONFIGURATION
# ========================
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
DEFAULT_FROM_EMAIL=noreply@uhcare.com.np

# ========================
# CELERY & REDIS (optional, for async tasks)
# ========================
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# ========================
# PAYMENT & NOTIFICATIONS
# ========================
PAYMENT_QR_CODE_URL=
PAYMENT_ACCOUNT_NAME=UH Care
PAYMENT_ACCOUNT_NUMBER=0000-0000-0000

# ========================
# THIRD-PARTY API KEYS
# ========================
DEFAULT_MODEL=gpt-5-mini

# Twilio SMS (optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ========================
# NOTES FOR PYTHONANYWHERE
# ========================
# 1. Set these environment variables in the Web tab (not in a .env file on PythonAnywhere)
# 2. Generate a secure DJANGO_SECRET_KEY: https://miniwebtool.com/django-secret-key-generator/
# 3. For Gmail: use an App Password (not your regular password); enable 2FA and generate app password
# 4. For DATABASE_URL: PythonAnywhere offers MySQL; format shown above
# 5. Ensure DJANGO_ALLOWED_HOSTS matches your PythonAnywhere domain exactly
# 6. Test locally with .env file (use python-dotenv or source it) before deploying
